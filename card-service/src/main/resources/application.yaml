server:
  port: 8082

spring:
  application:
    name: emsp-card-service
  cloud:
    nacos:
      server-addr: localhost:8848

logging:
  level:
    com.volvo.emspmicroservice.cardservice.mapper: debug

mybatis-plus:
  type-enums-package: com.volvo.emspmicroservice.cardservice.enumType
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler

custom:
  id-config:
    country-codes: CN,US,UK,JP,KR,DE,RU,CA
    vendor-codes: AB,CD,EF,GH,IJ,KL,MN

amqp:
  rabbitmq:
    host: rabbitmq-server
    port: 5672
    username: guest
    password: guest
    vhost: emspvhost
    routing:
      key: emsp.message.key
    exchange:
      name: emsp.exchange.fanout
    queue:
      name: emsp.consumer.queue